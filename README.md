# Snake

Minimal classic Snake implemented as a static browser app with no external dependencies.

## Run

```bash
node server.mjs
```

Then open `http://localhost:3000`.

## Test

```bash
node tests/runTests.js
```